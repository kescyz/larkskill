---
name: lark-base
description: LarkSuite Bitable database management - tables, records, fields, views, permissions. Use when user asks about databases, Bitable, or data records. Requires MCP setup via `npx larkskill install`.
---

# Lark Base

Manage Lark Bitable databases: create tables, define fields (26+ types), manage records (CRUD + batch), configure views, and set advanced permissions with custom roles.

## Prerequisites

- LarkSkill MCP configured — run `npx larkskill install` or see https://larkskill-portal.pages.dev/setup
- [api-validation.md](./references/api-validation.md) — Field types, constraints, rate limits, error codes